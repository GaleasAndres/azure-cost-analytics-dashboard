"""Utilities for working with Azure resource IDs."""

from typing import Dict


def parse_resource_id(resource_id: str) -> Dict[str, str]:
    """Parse an Azure resource ID into its component parts."""
    return {}
